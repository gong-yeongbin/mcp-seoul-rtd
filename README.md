# mcp-seoul-rtd

서울 실시간 데이터를 제공하는 MCP 서버입니다.

> 사용할 API 는 아직 확정되지 않았습니다. 확정되면 tool 목록과 설정 방법이 여기에 추가됩니다.

## 개발

```bash
pnpm install
pnpm typecheck   # 타입 검사
pnpm test        # node --test (Node 23.6+ 필요)
pnpm build       # tsc → dist/
```
